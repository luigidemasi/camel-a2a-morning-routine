apiVersion: v1
kind: Service
metadata:
  name: dashboard
  labels:
    app: a2a-morning-routine
    component: dashboard
spec:
  type: ClusterIP
  selector:
    app: dashboard
  ports:
    - name: http
      port: 3000
      targetPort: 3000
