apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: dashboard
  labels:
    app: a2a-morning-routine
    component: dashboard
spec:
  to:
    kind: Service
    name: dashboard
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
