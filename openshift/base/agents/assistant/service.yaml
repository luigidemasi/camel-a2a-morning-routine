apiVersion: v1
kind: Service
metadata:
  name: assistant
  labels:
    app: a2a-morning-routine
    component: assistant
spec:
  type: ClusterIP
  selector:
    app: assistant
  ports:
    - name: http
      port: 8090
      targetPort: 8090
