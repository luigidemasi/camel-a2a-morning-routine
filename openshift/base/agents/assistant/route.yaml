apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: assistant
  labels:
    app: a2a-morning-routine
    component: assistant
spec:
  to:
    kind: Service
    name: assistant
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
