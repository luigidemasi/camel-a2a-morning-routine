apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: breakfast-agent
  labels:
    app: a2a-morning-routine
    component: breakfast-agent
spec:
  source:
    type: Git
    git:
      uri: https://github.com/luigidemasi/camel-a2a-morning-routine.git
      ref: main
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: dockerfiles/camel-agent.Dockerfile
      buildArgs:
        - name: AGENT_NAME
          value: breakfast-agent
        - name: CAMEL_VERSION
          value: 4.21.0-SNAPSHOT
  output:
    to:
      kind: ImageStreamTag
      name: breakfast-agent:latest
  triggers:
    - type: ConfigChange
