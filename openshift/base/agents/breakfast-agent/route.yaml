apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: breakfast-agent
  labels:
    app: a2a-morning-routine
    component: breakfast-agent
spec:
  to:
    kind: Service
    name: breakfast-agent
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
