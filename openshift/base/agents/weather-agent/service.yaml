apiVersion: v1
kind: Service
metadata:
  name: weather-agent
  labels:
    app: a2a-morning-routine
    component: weather-agent
spec:
  type: ClusterIP
  selector:
    app: weather-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
