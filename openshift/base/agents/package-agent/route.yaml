apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: package-agent
  labels:
    app: a2a-morning-routine
    component: package-agent
spec:
  to:
    kind: Service
    name: package-agent
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
