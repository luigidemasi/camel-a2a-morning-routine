apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - buildconfig.yaml
  - imagestream.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  - configmap.yaml

commonLabels:
  app: a2a-morning-routine
  component: traffic-agent
