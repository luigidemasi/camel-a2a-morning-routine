apiVersion: v1
kind: Service
metadata:
  name: traffic-agent
  labels:
    app: a2a-morning-routine
    component: traffic-agent
spec:
  type: ClusterIP
  selector:
    app: traffic-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
