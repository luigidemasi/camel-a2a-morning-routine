apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: traffic-agent
  labels:
    app: a2a-morning-routine
    component: traffic-agent
spec:
  source:
    type: Git
    git:
      uri: https://github.com/YOUR_USERNAME/camel-a2a-morning-routine.git
      ref: main
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: dockerfiles/camel-agent.Dockerfile
      buildArgs:
        - name: AGENT_NAME
          value: traffic-agent
        - name: CAMEL_VERSION
          value: 4.21.0-SNAPSHOT
  output:
    to:
      kind: ImageStreamTag
      name: traffic-agent:latest
  triggers:
    - type: ConfigChange
