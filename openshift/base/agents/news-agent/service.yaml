apiVersion: v1
kind: Service
metadata:
  name: news-agent
  labels:
    app: a2a-morning-routine
    component: news-agent
spec:
  type: ClusterIP
  selector:
    app: news-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
