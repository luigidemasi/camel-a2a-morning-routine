apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: keycloak
  labels:
    app: a2a-morning-routine
    component: keycloak
spec:
  to:
    kind: Service
    name: keycloak
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
