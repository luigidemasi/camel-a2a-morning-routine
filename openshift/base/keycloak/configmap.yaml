apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-realm
  labels:
    app: a2a-morning-routine
    component: keycloak
data:
  realm.json: |
    {
      "realm": "a2a-morning-routine",
      "enabled": true,
      "clients": [
        {
          "clientId": "weather-agent",
          "enabled": true,
          "clientAuthenticatorType": "client-secret",
          "secret": "**GENERATED**",
          "serviceAccountsEnabled": true,
          "directAccessGrantsEnabled": false,
          "publicClient": false,
          "protocol": "openid-connect"
        },
        {
          "clientId": "news-agent",
          "enabled": true,
          "clientAuthenticatorType": "client-secret",
          "secret": "**GENERATED**",
          "serviceAccountsEnabled": true,
          "directAccessGrantsEnabled": false,
          "publicClient": false,
          "protocol": "openid-connect"
        },
        {
          "clientId": "assistant-agent",
          "enabled": true,
          "clientAuthenticatorType": "client-secret",
          "secret": "**GENERATED**",
          "serviceAccountsEnabled": true,
          "directAccessGrantsEnabled": false,
          "publicClient": false,
          "protocol": "openid-connect"
        },
        {
          "clientId": "traffic-agent",
          "enabled": true,
          "clientAuthenticatorType": "client-secret",
          "secret": "**GENERATED**",
          "serviceAccountsEnabled": true,
          "directAccessGrantsEnabled": false,
          "publicClient": false,
          "protocol": "openid-connect"
        },
        {
          "clientId": "email-agent",
          "enabled": true,
          "clientAuthenticatorType": "client-secret",
          "secret": "**GENERATED**",
          "serviceAccountsEnabled": true,
          "directAccessGrantsEnabled": false,
          "publicClient": false,
          "protocol": "openid-connect"
        }
      ]
    }
